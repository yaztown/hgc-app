import { ControllerHostDirective } from './controller-host.directive';

describe('ControllerHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ControllerHostDirective();
    expect(directive).toBeTruthy();
  });
});
