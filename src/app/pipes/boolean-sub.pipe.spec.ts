import { BooleanSubPipe } from './boolean-sub.pipe';

describe('BooleanSubPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanSubPipe();
    expect(pipe).toBeTruthy();
  });
});
